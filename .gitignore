# Node & package managers
node_modules/
pnpm-lock.yaml
yarn.lock
package-lock.json

# Next.js / Turbopack / Vercel
.next/
.turbo/
.vercel/
out/

# TypeScript
*.tsbuildinfo

# Logs & temp
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
*.log
.DS_Store
*.swp
*.swo

# Env & secrets
.env
.env.*           # includes .env.local etc.
*.local

# IDE
.vscode/
.idea/

# Project runtime data (generated at runtime, not source)
data/
inbound_emails/

# Keep placeholders so folders exist
!data/.gitkeep
!inbound_emails/.gitkeep

# Next.js app build artifacts inside sentinel-web
sentinel-web/.next/
sentinel-web/out/
sentinel-web/.vercel/

# Test coverage / caches
coverage/
.cache/

# Mail parsing caches or temp (if any appear)
*.eml.tmp
*.tmp